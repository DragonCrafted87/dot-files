[Service]
# FUSE unmount of /run/user/$UID/doc often ignores SIGTERM (fusermount3
# auto_unmount). Default 90s stop plus distro TimeoutStopFailureMode=abort
# delays logout and dumps a core.
TimeoutStopSec=5
TimeoutStopFailureMode=kill
